Plugins can read a configuration value that may be unset through ``ConfigSet.get_optional``, which verifies the value
against its declared type and returns ``None`` when it is not set - by :user:`gaborbernat`.
