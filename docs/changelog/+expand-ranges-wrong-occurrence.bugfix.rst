Expand the range that actually matched in a factor expression instead of the first identical digit-range substring
found in the value, so a literal ``2-4`` appearing before a ``{2-4}`` range is no longer expanded by mistake.
